# Album card

This component is a extentions of [CardComponent](?path=/docs/card-cardcomponent--default)

It's meant to make easier use of [Spotify Albums](https://developer.spotify.com/documentation/web-api/reference/#endpoint-get-an-album) received data.

import { Story, Canvas } from '@storybook/addon-docs';

## Example use

This component take only one argument `album`.

<Canvas>
  <Story id="card-extended-albumcard--default" />
</Canvas>
